# Train_booking_server
After you compile the code, you can run the server with following command:
```
$ ./write_server {port}
$ ./read_server {port}
```
It's just a easy system for train booking and reservation.
